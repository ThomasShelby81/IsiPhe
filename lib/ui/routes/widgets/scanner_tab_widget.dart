import 'package:flutter/material.dart';

class ScannerTab extends Tab {
  const ScannerTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
